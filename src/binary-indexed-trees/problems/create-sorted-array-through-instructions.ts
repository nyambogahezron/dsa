/**
 * @file create-sorted-array-through-instructions.ts
 * @description 
 * Given an integer array `instructions`, you are asked to create a sorted array from them. You start with an empty container `nums`. For each element from left to right in instructions, insert it into nums. The cost of each insertion is the minimum of the following:
 * - The number of elements currently in nums that are strictly less than instructions[i].
 * - The number of elements currently in nums that are strictly greater than instructions[i].
 * Return the total cost to insert all elements from instructions. Since the answer may be large, return it modulo 10^9 + 7.
 * 
 * Constraints:
 * - 1 <= instructions.length <= 10^5
 * - 1 <= instructions[i] <= 10^5
 */

export function createSortedArray(instructions: number[]): number {
    // Your implementation goes here
    return 0;
}
