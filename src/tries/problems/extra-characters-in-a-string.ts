/**
 * @file extra-characters-in-a-string.ts
 * @description 
 * You are given a 0-indexed string `s` and a dictionary of strings `dictionary`. You have to break `s` into one or more non-overlapping substrings such that each substring is present in dictionary. There may be some extra characters in s which are not present in any of the substrings.
 * Return the minimum number of extra characters left over if you break up s optimally.
 * 
 * Constraints:
 * - 1 <= s.length <= 50
 * - 1 <= dictionary.length <= 50
 * - 1 <= dictionary[i].length <= 50
 * - dictionary[i] and s consists of lowercase English letters.
 * - dictionary contains distinct strings.
 */

export function minExtraChar(s: string, dictionary: string[]): number {
    // Your implementation goes here
    return 0;
}
