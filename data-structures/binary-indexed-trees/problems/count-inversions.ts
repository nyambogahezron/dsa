/**
 * @file count-inversions.ts
 * @description 
 * Given an array of integers `nums`, return the number of inversions in the array.
 * Two elements `nums[i]` and `nums[j]` form an inversion if `i < j` and `nums[i] > nums[j]`.
 * 
 * Constraints:
 * - 1 <= nums.length <= 5 * 10^4
 * - -10^5 <= nums[i] <= 10^5
 */

export function countInversions(nums: number[]): number {
    // Your implementation goes here
    return 0;
}
