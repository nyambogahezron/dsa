/**
 * @file string-match-brute.ts
 * @description Naive Substring Search
 * 
 * --- Problem Description ---
 * Given a text string `text` and a pattern string `pattern`, return the index of the first occurrence of `pattern` in `text`.
If pattern is not part of text, return -1. Implement this exhaustively by sliding the pattern cell by cell.
 * 
 * --- Example ---
 * Input: text = "hello", pattern = "ll"
Output: 2
 * 
 * 
 * --- Constraints ---
 * - Follow the canonical constraints for this problem statement.
 * - Validate minimal input, typical input, and upper-bound input sizes.
 * - Ensure behavior is correct for duplicates, ordering, and empty-like edge cases where applicable.
 * 
 * --- Target Complexity ---
 * Time Complexity: O(N * M) (where N is text length, M is pattern length)
Space Complexity: O(1)
 * 
 * --- Test Cases ---
 * 1. Basic Case
 *    Input: Use a representative normal input from the example.
 *    Expected: Correct output for the standard scenario.
 * 2. Edge Case
 *    Input: Minimal valid input (e.g., empty/single element/smallest grid).
 *    Expected: Correct base-condition behavior.
 * 3. Boundary Case
 *    Input: Large valid input near constraints.
 *    Expected: Correct output within target complexity bounds.
 */

export function naiveSubstringSearch(text: string, pattern: string): number {
    // TODO: Implement the solution for Naive Substring Search
    throw new Error("Method not implemented.");
}

// Sample Test Scaffold (enable after implementation)
// console.log(naiveSubstringSearch[0m(/* TODO: add test input */));
