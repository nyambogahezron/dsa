/**
 * @file three-sum.ts
 * @description 3Sum
 * 
 * --- Problem Description ---
 * Given an integer array `nums`, return all the unique triplets `[nums[i], nums[j], nums[k]]` such that `i !== j`, `i !== k`, and `j !== k`, and `nums[i] + nums[j] + nums[k] === 0`.
Notice that the solution set must not contain duplicate triplets. You must sort the array first.
 * 
 * --- Example ---
 * Input: nums = [-1, 0, 1, 2, -1, -4]
Output: [[-1, -1, 2], [-1, 0, 1]]
 * 
 * --- Target Complexity ---
 * Time Complexity: O(N^2)
Space Complexity: O(1) auxiliary (ignoring sorting output space)
 */

export function threeSum(nums: number[]): number[][] {
    // TODO: Implement the solution for 3Sum
    throw new Error("Method not implemented.");
}
